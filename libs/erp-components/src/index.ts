/**
 * `@org/erp-components` — the composite layer.
 *
 * Nothing is exported yet. The seven composites this package exists for are
 * queued in IMPLEMENTATION.md; see AGENTS.md for what makes a component belong
 * here rather than in `@org/design-library`.
 */
export {};
